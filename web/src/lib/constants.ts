/** localStorage key for the clergy identity token */
export const ME_TOKEN_KEY = 'vh_me_token';

// Quality gate for default view (extended positions)
export const QUALITY_GATE_THRESHOLD = 85;

// "Is new" thresholds
export const NEW_RECEIVING_NAMES_DAYS = 30;
export const NEW_UPDATED_ON_HUB_DAYS = 14;

// Match tier thresholds (0-100 scale)
export const MATCH_TIER_STRONG = 75;
export const MATCH_TIER_GOOD = 50;
export const MATCH_TIER_PARTIAL = 25;

// Recency window for default view filter (months)
export const RECENCY_MONTHS = 12;
