name: Monthly Data Refresh v2

on:
  schedule:
    - cron: '0 5 1 * *'
  workflow_dispatch:

env:
  DB_RELEASE_TAG: db-latest

concurrency:
  group: data-collection
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Chromium
        working-directory: scraper
        run: npx playwright install chromium --with-deps

      - name: Download DB
        id: download-db
        run: |
          mkdir -p data
          if gh release view ${{ env.DB_RELEASE_TAG }} &>/dev/null; then
            gh release download ${{ env.DB_RELEASE_TAG }} -p vocationhub.db -D data/
            echo "db_found=true" >> $GITHUB_OUTPUT
          else
            echo "::notice::No DB release found -- starting fresh"
            echo "db_found=false" >> $GITHUB_OUTPUT
          fi
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Scrape Asset Map (Episcopal church directory)
        run: npm run church-directory --workspace=scraper
        timeout-minutes: 60
        env:
          VOCATIONHUB_DB_PATH: ${{ github.workspace }}/data/vocationhub.db

      - name: Import Asset Map
        run: node web/scripts/import-asset-map.js
        env:
          VOCATIONHUB_DB_PATH: ${{ github.workspace }}/data/vocationhub.db

      - name: Fetch ECDPlus parishes
        run: node web/scripts/fetch-ecdplus-parishes.js
        timeout-minutes: 30
        env:
          VOCATIONHUB_DB_PATH: ${{ github.workspace }}/data/vocationhub.db

      - name: Fetch ECDPlus clergy
        run: node web/scripts/fetch-ecdplus-clergy.js
        timeout-minutes: 30
        env:
          VOCATIONHUB_DB_PATH: ${{ github.workspace }}/data/vocationhub.db

      - name: Merge parishes
        run: node web/scripts/merge-parishes.js
        env:
          VOCATIONHUB_DB_PATH: ${{ github.workspace }}/data/vocationhub.db

      - name: Populate parish identity table
        run: node web/scripts/populate-parish-identity.js
        env:
          VOCATIONHUB_DB_PATH: ${{ github.workspace }}/data/vocationhub.db

      - name: Fetch Census Bureau data
        run: node web/scripts/fetch-census-data.js
        timeout-minutes: 15
        env:
          VOCATIONHUB_DB_PATH: ${{ github.workspace }}/data/vocationhub.db

      - name: Scrape parochial reports
        run: npm run parochial-reports --workspace=scraper
        timeout-minutes: 30
        env:
          VOCATIONHUB_DB_PATH: ${{ github.workspace }}/data/vocationhub.db

      - name: Import parochial data
        run: node web/scripts/import-parochial-data.js
        env:
          VOCATIONHUB_DB_PATH: ${{ github.workspace }}/data/vocationhub.db

      - name: Validate DB before upload
        run: |
          node -e "
            const Database = require('better-sqlite3');
            const db = new Database('data/vocationhub.db');
            const tables = db.prepare(\"SELECT name FROM sqlite_master WHERE type='table'\").all().map(r => r.name);
            const required = ['scraped_positions', 'scraper_meta'];
            for (const t of required) {
              if (!tables.includes(t)) { console.error('Missing required table: ' + t); process.exit(1); }
            }
            const posCount = db.prepare('SELECT COUNT(*) as c FROM scraped_positions').get().c;
            console.log('Positions in DB: ' + posCount);
            if (posCount === 0) { console.error('DB has 0 positions -- refusing to upload'); process.exit(1); }
            db.close();
          "

      - name: Upload vocationhub.db to release
        run: gh release upload ${{ env.DB_RELEASE_TAG }} data/vocationhub.db --clobber
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Trigger build
        run: gh api repos/${{ github.repository }}/dispatches -f event_type=build
        env:
          GH_TOKEN: ${{ github.token }}
